# Laravel API base (include /api/v1). Must match the host:port you start the backend on.
VITE_API_BASE_URL=http://127.0.0.1:8001/api/v1

# Must match backend INTERNTRACK_CURRENT_TERM / config('interntrack.current_term')
VITE_INTERNTRACK_CURRENT_TERM=AY 2025-2026, Sem 2
