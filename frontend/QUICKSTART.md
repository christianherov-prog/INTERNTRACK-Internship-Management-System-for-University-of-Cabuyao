# ARCHIVED — Quick Start (superseded)

This file is **outdated** and kept only as a pointer. It previously referenced the old Interntrack-UI CSS copy workflow.

Use these docs instead:

- **[Full stack setup](../SETUP.md)** — backend, database, env, and frontend in one place
- **[Frontend README](./README.md)** — Vite dev server, routes, and frontend-specific notes
- **[Frontend setup](./SETUP.md)** — npm install and local `.env` for the React app

Quick dev (from this folder):

```bash
npm install
cp .env.example .env
npm run dev
```

Default password for demo accounts: `interntrack123` (see root [SETUP.md](../SETUP.md) for IDs).
